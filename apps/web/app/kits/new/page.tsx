"use client";
import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {api} from "../../../lib/api";
export default function NewKit(){const [jd,setJd]=useState(""),[url,setUrl]=useState(""),[days,setDays]=useState(5),[error,setError]=useState("");const r=useRouter();async function submit(e:FormEvent){e.preventDefault();setError("");try{const d=await api("/api/kits",{method:"POST",body:JSON.stringify({jd,company_url:url,days})});r.push(`/kits/${d.kit._id}`)}catch(e:any){setError(e.message)}}return <main className="min-h-screen p-6 md:p-10 max-w-4xl mx-auto"><a href="/dashboard" className="muted">← Dashboard</a><h1 className="text-3xl font-bold mt-4 mb-8">Create interview kit</h1><form onSubmit={submit} className="card p-6 space-y-6"><div><label className="label">Job description</label><textarea className="input min-h-72" value={jd} onChange={e=>setJd(e.target.value)} placeholder="Paste the complete job description here…" required/></div><div><label className="label">Company website</label><input className="input" type="url" value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://example.com" required/></div><div><label className="label">Days until interview</label><input className="input" type="number" min={1} max={365} value={days} onChange={e=>setDays(Number(e.target.value))} required/></div>{error&&<p className="text-red-400">{error}</p>}<button className="btn btn-primary">Generate kit</button></form></main>}
