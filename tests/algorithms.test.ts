import { describe, expect, it } from "vitest";
import { allocateSchedule, checkCoverage, validateKit } from "@prep-kit/core";

describe("coverage checker",()=>{it("finds uncovered must requirements",()=>{const req=[{id:"r1",text:"React",kind:"technical" as const,priority:"must" as const},{id:"r2",text:"AWS",kind:"technical" as const,priority:"must" as const},{id:"r3",text:"Bonus",kind:"domain" as const,priority:"nice" as const}];const qs=[{id:"q1",requirement_ids:["r1"],category:"technical" as const,prompt:"x",answer_outline:"y",difficulty:1 as const}];expect(checkCoverage(req,qs)).toEqual(["r2"])});
});
describe("schedule",()=>{it("creates exactly the requested number of days and valid references",()=>{const req=[{id:"r1",text:"React",kind:"technical" as const,priority:"must" as const}];const qs=[1,2,3].map((n)=>({id:`q${n}`,requirement_ids:["r1"],category:"technical" as const,prompt:"x",answer_outline:"y",difficulty:2 as const}));const days=allocateSchedule(req,qs,5);expect(days).toHaveLength(5);expect(days.flatMap(d=>d.question_ids)).toEqual(["q1","q2","q3"]);expect(days.every(d=>Number.isInteger(d.minutes))).toBe(true)});
});
describe("kit structure",()=>{it("accepts the required Appendix A shape",()=>{const kit={source:{company:"Acme",company_url:"https://acme.test",role:"Engineer",location:"",jd_chars:2,researched_at:new Date().toISOString(),pages_used:[]},company_brief:{summary:"x",what_they_do:"y",sources:[]},role:{title:"Engineer",seniority:"",responsibilities:[],requirements:[{id:"r1",text:"React",kind:"technical" as const,priority:"must" as const}]},questions:[{id:"q1",requirement_ids:["r1"],category:"technical" as const,prompt:"x",answer_outline:"y",difficulty:1 as const}],flashcards:[{id:"f1",front:"x",back:"y",requirement_ids:["r1"]}],schedule:{days_available:1,days:[{day:1,focus:"x",question_ids:["q1"],minutes:30}]},coverage:{uncovered_requirement_ids:[],passes:2}};expect(()=>validateKit(kit)).not.toThrow();});});
